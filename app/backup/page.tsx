import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export const metadata = { title: "Backup" };

export default function BackupPage() {
  return <PlaceholderPage eyebrow="Segurança dos dados" title="Backup" description="A exportação de segurança será implementada na fase prevista pelo roadmap." />;
}
